language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ng-require' ]; then cd .. && eval "mv $currentfolder generator-ng-require" && cd generator-ng-require; fi

